{
  "name": "cete-tasks",
  "version": "0.1.0",
  "description": "Agent-facing task tools (assign/claim/complete/...) for Claude Code — a thin MCP client over cete-os-server's task REST API.",
  "license": "MIT",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
