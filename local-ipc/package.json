{
  "name": "claude-local-ipc-channel",
  "version": "0.3.0",
  "description": "1:1 local message channel for co-located Claude Code agents over a filesystem queue.",
  "license": "MIT",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
